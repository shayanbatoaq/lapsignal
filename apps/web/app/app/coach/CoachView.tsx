"use client";
import { RefreshCcw,ShieldCheck,Sparkles } from "lucide-react";
import { useState } from "react";
import { Confidence,EvidenceCard,SectionHeading,StateCard } from "@/components/UI";
import type { CoachReport,Session } from "@/lib/types";
const API=process.env.NEXT_PUBLIC_API_BASE_URL??"http://localhost:8000";
export function CoachView({session}:{session:Session}){const [report,setReport]=useState<CoachReport>(session.report);const [loading,setLoading]=useState(false);const [error,setError]=useState<string|null>(null);const generate=async(regenerate=false)=>{setLoading(true);setError(null);try{const response=await fetch(`${API}/v1/sessions/${session.id}/coach?regenerate=${regenerate}`,{method:"POST"});if(!response.ok)throw new Error("Coach request failed");setReport(await response.json())}catch{setError("The coaching service could not be reached. Your saved deterministic analysis is still available.")}finally{setLoading(false)}};const cloud=report.mode==="openrouter";return <><div className="session-detail-head"><div><div className={`tag ${cloud?"":"local"}`}>{cloud?<Sparkles size={12}/>:<ShieldCheck size={12}/>} {report.label}</div><h2>Post-stint debrief</h2><p>{session.track_name} Â· {session.session_type} Â· {session.input_device} Â· {session.performance_mode??"unknown"} performance</p></div><div className="detail-actions"><button className="button" disabled={loading} onClick={()=>generate(false)}><Sparkles size={14}/>{loading?"Validating evidenceâ€¦":"Generate AI debrief"}</button>{cloud&&<button className="button secondary small" disabled={loading} onClick={()=>generate(true)}><RefreshCcw size={14}/> Regenerate</button>}</div></div><div className="coach-layout"><div><div className="surface-card" style={{marginBottom:16}}><SectionHeading eyebrow={cloud?"OpenRouter AI explanation":"Deterministic fallback"} title="Session summary"/><p className="coach-summary">{report.summary??report.session_summary}</p>{error&&<StateCard title="Coach unavailable">{error}</StateCard>}{report.explanation&&<StateCard title="Fallback status">{report.explanation}</StateCard>}<div className="state-card"><ShieldCheck size={20}/><div><strong>Deterministic analysis is the source of truth</strong><p>Every coaching action must cite Evidence Bundle v1 IDs. Raw telemetry is never sent to the model.</p></div></div>{cloud&&<p className="muted mono" style={{marginTop:12,fontSize:11}}>OpenRouter Â· {String(report.provenance.resolved_model)} Â· Evidence bundle v{String(report.provenance.evidence_bundle_version)} Â· {report.provenance.cached?"Cached":"Generated"} Â· {new Date(String(report.provenance.generated_at)).toLocaleString()}</p>}</div>{report.priority_actions?.length?<><SectionHeading eyebrow="Validated structured output" title="AI priority actions"/><div className="coach-priorities">{report.priority_actions.map(action=><article className="evidence-card" key={action.priority}><div className="evidence-top"><span className="priority-index">P{action.priority}</span>{typeof action.confidence==="number"?<Confidence value={action.confidence}/>:<span className="tag mono">Evidence-backed</span>}</div><h3>{action.title}</h3>{action.observation&&<p>{action.observation}</p>}<p><strong>{action.location}</strong> Â· {action.reason}</p><div className="action-strip"><span>Next action</span><strong>{action.instruction}</strong></div><small className="muted mono">Evidence: {action.evidence_ids.join(", ")}</small></article>)}</div></>:<><SectionHeading eyebrow="Deterministic analysis" title="Priority calls"/><div className="coach-priorities">{session.findings.slice(0,3).map(finding=><EvidenceCard finding={finding} key={finding.id}/>)}</div></>}</div><aside className="coach-side"><div className="surface-card"><SectionHeading eyebrow="Coaching modes" title="Clear provenance"/><div className="technical-list"><Row label="Analysis" value="Deterministic"/><Row label="Coach" value={cloud?"OpenRouter AI":"Rule-based"}/><Row label="Provider" value={String(report.provenance.provider??"local")}/><Row label="Cache" value={report.provenance.cached?"Hit":"Miss / local"}/></div></div><div className="surface-card"><SectionHeading eyebrow="Privacy" title="Bounded evidence only"/><p className="coach-summary">No raw UDP, file paths, IP addresses, participant name, key, or hidden reasoning is included.</p></div></aside></div></>}
function Row({label,value}:{label:string;value:string}){return <div className="technical-row"><span>{label}</span><strong>{value}</strong></div>}
